import { render, screen } from '@testing-library/react';
import MetaDisplayer from './MetaDisplayer';

test('should display metadata correctly', () => {
  // const metadata = [
  //   { title: 'Example', description: 'Example description', image: 'http://example.com/image.jpg' }
  // ];

  // render(<MetaDisplayer metadata={metadata} />);

  // expect(screen.getByText(/Example/i)).toBeInTheDocument();
  // expect(screen.getByText(/Example description/i)).toBeInTheDocument();
  // expect(screen.getByAltText(/Example/i)).toBeInTheDocument();
});
