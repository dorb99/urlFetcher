import { render, screen, fireEvent } from '@testing-library/react';
import FormContainer from './FormContainer';

test('should allow users to submit URLs', () => {
  // const submitUrls = jest.fn();
  // render(<FormContainer submitUrls={submitUrls} />);

  // fireEvent.change(screen.getAllByPlaceholderText(/Enter URL/i)[0], { target: { value: 'http://example.com' } });
  // fireEvent.click(screen.getByText(/Submit/i));

  // expect(submitUrls).toHaveBeenCalledWith(['http://example.com', '', '']);
});
